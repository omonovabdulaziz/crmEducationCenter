package it.live.crm.repository;

import it.live.crm.entity.StudentArchiveGroup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StudentArchivedGroupRepository extends JpaRepository<StudentArchiveGroup, Long> {
}
